// src/services/index.ts

// Only export essential interfaces that might be needed
export * from '../interfaces/content';

// No need for adapter exports or initialization functions
// All the actual functionality is now handled directly by metadataFetcher.ts

// If anything else needs to be exported for compatibility reasons, we can add it here